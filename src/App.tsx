// Placeholder shell. No UI is built yet — the spine (schema, scoring, tests) comes first.
export default function App() {
  return (
    <main className="p-8 font-mono text-sm">
      ONA — perinatal risk registry. Spine under construction.
    </main>
  )
}
